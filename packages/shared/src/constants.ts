export const SUPPORTED_LOCALES = ["en", "ru", "uk", "de", "fr", "it"] as const;

export const DEFAULT_LOCALE = "en";

export const DEFAULT_CURRENCY = "USD";

// TODO: Replace placeholder project categories with product-approved values.
export const PROJECT_CATEGORIES = ["website", "saas", "mobile-app", "automation"] as const;
