export const packageConfig = {
  typescript: {
    strict: true,
    moduleResolution: "bundler",
  },
} as const;

// TODO: Add shared ESLint and Prettier config once the repo standard is finalized.
