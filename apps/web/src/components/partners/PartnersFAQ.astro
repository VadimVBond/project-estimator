---
const { items } = Astro.props;
---

{items && items.length > 0 && (
  <section class="faq">
    <h2>FAQ</h2>
    <ul>
      {items.map(item => (
        <li>
          <strong>{item.q}</strong>: {item.a}
        </li>
      ))}
    </ul>
  </section>
)}
