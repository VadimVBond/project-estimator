---
import Layout from '~/layouts/LandingLayout.astro';

import Hero2 from '~/components/widgets/Hero2.astro';
import CallToAction from '~/components/widgets/CallToAction.astro';

import { getLocaleFromUrl } from '~/i18n/translator';
import { lpByLocale } from '~/i18n/lp';

const locale = getLocaleFromUrl(Astro.url);
const lp = (lpByLocale[locale] || lpByLocale['en']).landings;

const metadata = {
  title: lp.sales.metadata.title,
};
---

<Layout metadata={metadata}>
  <!-- Hero2 Widget ******************* -->

  <Hero2
    tagline={lp.sales.hero.tagline}
    title={lp.sales.hero.title}
    subtitle={lp.sales.hero.subtitle}
    callToAction={{ text: lp.sales.hero.cta, href: '#', icon: 'tabler:square-rounded-arrow-right' }}
    callToAction2={{ text: lp.sales.hero.cta2, href: '#' }}
    image={{
      src: 'https://images.unsplash.com/photo-1621452773781-0f992fd1f5cb?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1626&q=80',
      alt: lp.sales.hero.imageAlt,
    }}
  />

  <CallToAction
    title={lp.sales.comingSoon.title}
    subtitle={lp.sales.comingSoon.subtitle}
    callToAction={{
      text: lp.sales.comingSoon.buttonText,
      href: 'https://github.com/onwidget/astrowind',
      icon: 'tabler:download',
    }}
  />
</Layout>
