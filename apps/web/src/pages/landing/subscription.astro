---
import Layout from '~/layouts/LandingLayout.astro';

import Hero2 from '~/components/widgets/Hero2.astro';
import CallToAction from '~/components/widgets/CallToAction.astro';

import { getLocaleFromUrl } from '~/i18n/translator';
import { lpByLocale } from '~/i18n/lp';

const locale = getLocaleFromUrl(Astro.url);
const lp = (lpByLocale[locale] || lpByLocale['en']).landings;

const metadata = {
  title: lp.subscription.metadata.title,
};
---

<Layout metadata={metadata}>
  <!-- Hero2 Widget ******************* -->

  <Hero2
    tagline={lp.subscription.hero.tagline}
    title={lp.subscription.hero.title}
    subtitle={lp.subscription.hero.subtitle}
    callToAction={{ text: lp.subscription.hero.cta, href: '#', icon: 'tabler:square-rounded-arrow-right' }}
    callToAction2={{ text: lp.subscription.hero.cta2, href: '#' }}
    image={{
      src: 'https://images.unsplash.com/photo-1593510987046-1f8fcfc512a0?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80',
      alt: lp.subscription.hero.imageAlt,
    }}
  />

  <CallToAction
    title={lp.subscription.comingSoon.title}
    subtitle={lp.subscription.comingSoon.subtitle}
    callToAction={{
      text: lp.subscription.comingSoon.buttonText,
      href: 'https://github.com/onwidget/astrowind',
      icon: 'tabler:download',
    }}
  />
</Layout>
